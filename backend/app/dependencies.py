"""
dependencies.py — FastAPI Dependencies (Dependency Injection).

TODO:
- Tạo dependency `get_db()` trả về async DB session.
- Dùng `AsyncSession` từ SQLAlchemy async.
- Đảm bảo session được đóng sau mỗi request (yield pattern).
"""
