/**
 * useCafes.js — Custom Hook: Fetch Cafes / Nearby Cafes.
 *
 * TODO:
 * - fetchCafes() → gọi api.getCafes(), danh sách tĩnh.
 * - fetchNearbyCafes(lat, lng) → gọi api.getNearbyCafes(lat, lng).
 * - Quản lý state: loading, error, cafes[].
 * - Trả về: { cafes, loading, error, fetchCafes, fetchNearbyCafes }.
 * - Mọi API call qua services/api.js.
 */
